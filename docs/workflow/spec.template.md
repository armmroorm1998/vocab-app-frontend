# Spec: <feature name>

## Summary
One paragraph: what this feature does.

## Behavior
What the user sees and does, page by page or component by component. Include edge cases (empty states, errors, loading).

## Data
What data is read or written, and where it comes from (API route, local state, existing types in `src/types`).

## Out of scope
What this explicitly does not cover, to prevent scope creep.
